const nome = 'matheus'; // string
const nome1 = "matheus"; // string
const nome2 = `matheus`; // string
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAluno; // undefined = nao aponta papra lugar nenhum na memoria
const sobrenomeAluno = null; // nulo -> nao aponta para lugar nenhum na memoria
const aprovado = false // boolean = true, false (logico)


// typeof (confera qual tipo da variavel);

console.log(typeof nome, typeof num1);

let a = 2;
const b = a;

console.log(a, b);

a = 3;

console.log(a, b);
